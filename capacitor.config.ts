import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.WebXR',
  appName: 'WebXR',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
